function add(a, b) {
 var add = a + b;
  return(add);
}

function substract(a, b) {
  var substract = a - b;
  return(substract);
}

function multiply(a, b) {
  var multiply = a * b;
  return(multiply)
}

function divide(a, b) {
  var divide = a/b;
  return(divide);
}

function inc(n) {
  var inc = ++n;
  return(inc)
}